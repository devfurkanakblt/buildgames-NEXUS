{
  "name": "nexus-avalanche-teleporter",
  "version": "1.0.0",
  "description": "NEXUS: Native cross-chain recurring payment protocol on Avalanche",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:fuji": "hardhat run scripts/deploy.ts --network fuji"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.x",
    "typescript": "^5.x"
  }
}
